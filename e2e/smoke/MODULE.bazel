bazel_dep(name = "rules_debian_packages", version = "0.0.0", dev_dependency = True)
bazel_dep(name = "bazel_skylib", version = "1.5.0", dev_dependency = True)

local_path_override(
    module_name = "rules_debian_packages",
    path = "../..",
)
